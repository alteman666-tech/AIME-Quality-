# 演示操作路径

本路径同时用于招聘评审快速体验和 60–180 秒演示视频录制。它只规定页面操作与应展示的证据，不是答辩讲稿。

## 1. 录制前检查

- 使用中国大陆以外 IP，桌面版 Chrome 或 Edge 打开 [https://fuyao.edgeone.dev/](https://fuyao.edgeone.dev/)。
- 使用无痕窗口确认首页无需登录。
- 打开 `/api/health`，确认 `configured.fuyao=true`、`configured.qwen=true`、`ready=true`。
- 预留至少 3 次千问调用额度：C02 评测、C12 候选生成、C12 基线和候选复评。
- 浏览器缩放建议 80%–90%，确保结果卡和 Run ID 同屏。
- 关闭包含环境变量、API Key、个人账号和本地文件路径的页面。

## 2. 推荐体验路径

| 步骤 | 页面与操作 | 应看到的结果 | 证明的能力 | 建议时长 |
| --- | --- | --- | --- | --- |
| 1 | 打开治理概览 | 产品版本、规则、案例和治理模块 | 不是单一问答页，而是质量治理工作台 | 10 秒 |
| 2 | 进入评测工作台，选择 C02，点击“运行联合评测” | 170.90 亿元与 F01 的 1708.99 亿元不符，R02 阻断；显示模型、Judge、耗时和 Run | 真实模型评测、确定性数值检查、版本追踪 | 25 秒 |
| 3 | 向下查看事实证据链 | F01、营业收入、1708.99 亿元、年报来源和第 5 页链接 | 关键数字可追溯 | 10 秒 |
| 4 | 点击“加入人工复核”，填写一条理由并保存 | 机器初判保留，人工结论和理由单独出现 | 自动评测与人工验收分工 | 15 秒 |
| 5 | 打开画像对照 | C21 为 1708.99 亿元，C22 因高风险画像变成 2000 亿元；页面标出事实漂移 | 个性化只能改变表达，不能改变事实 | 15 秒 |
| 6 | 打开版本回归，选择 C12，保留候选 Prompt，点击“生成并回归” | 左侧基线实评不通过；右侧候选复评通过；出现基线评测、候选生成、候选评测三条 Run | Prompt 控制变量回归和修复闭环 | 35–50 秒 |
| 7 | 打开服务诊断 | 扶摇和千问已配置；行情查询返回业务数据和 request_id | 真实外部工具、业务码与失败透明 | 15 秒 |
| 8 | 回到 README 或概览的边界说明 | 构造数据、小样本、localStorage、非生产准确率等边界 | 主动说明限制，不夸大结果 | 10 秒 |

预计总时长 135–155 秒，符合题目要求的 60–180 秒。

## 3. 每一步的检查点

### 3.1 C02 数值错误

- 问题：贵州茅台 2024 年营业收入是多少亿元？
- 候选回答：170.90 亿元。
- 证据 F01：1708.99 亿元，贵州茅台 2024 年年度报告第 5 页。
- 预期：机器初判不通过，至少命中 R02；不能因为引用了正确页码而放过错误数值。

### 3.2 人工复核

推荐填写：`确认数量级错误；保留机器初判，修复回答后再回归。`

保存后确认：

- 人工结论没有覆盖机器原判。
- 刷新后当前浏览器仍能读取记录。
- 不展示真实姓名、账户或持仓。

### 3.3 C21/C22 画像对照

- 问题、证据、时间保持相同，只改变风险偏好。
- C21：1708.99 亿元，风险提示保留。
- C22：把事实改为 2000 亿元，属于 R02 数值矛盾和 R07 画像事实漂移。
- 需要说明：高风险偏好可以改变关注重点，不能改变财务事实，也不能消除收益不确定性。

### 3.4 C12 Prompt 回归

候选 Prompt：

> 回答前先核对证据中的主体、指标、期间、数值和单位；引用来源与页码。区分历史事实与未来判断，不承诺收益。按用户经验调整解释深度，但不得因风险偏好改变事实。

预期：

- 基线回答含“接下来必赚、忽略风险”，实评不通过。
- 候选回答引用 862.28 亿元和年报第 5 页，区分历史利润与未来收益并保留风险，复评通过。
- 页面出现三条新的 Run ID。模型输出具有轻微随机性，若复评仍不通过，应保留真实结果，不剪辑成预设成功；可检查 finding 后再次运行并说明原因。

### 3.5 服务诊断

- `/api/health` 的 ready=true 只代表环境变量已配置。
- 扶摇查询还要查看业务 `code=0`、data 和 request_id。
- 千问检查要看到模型名、非空正文和耗时。
- 不展开浏览器开发者工具中的请求头，避免泄漏账户信息。

## 4. 时间不足时的最短路径

若只剩 90 秒，保留四段：

1. C02 联合评测与 F01 证据。
2. C21/C22 画像对照。
3. C12 三段 Run 回归。
4. 服务诊断和已知边界。

人工复核可以展示已有记录，避免现场输入占用时间；但不能用静态截图替代 C02 或 C12 的真实操作。

## 5. 录制后检查

- 视频时长在 60–180 秒。
- 画面中能看清正式 URL、案例 ID、结论、证据和 Run ID。
- 使用的地址与 README 完全一致。
- 没有 API Key、环境变量值、个人邮箱、GitHub Token 或本地绝对路径。
- 没有把机器初判说成人工确认，也没有声称总体准确率。
- 视频链接在未登录窗口可打开。
