# AI 使用与验证记录

## 1. 使用原则

本作品由我主导产品问题定义、范围取舍、规则口径、案例验收、部署配置、生产测试和最终结论。AI 工具作为研发与分析助手，参与资料归纳、方案讨论、代码实现辅助、测试补充、问题排查和文档校对。

我没有把 AI 输出直接当作金融事实、人工标签或上线结论。关键数字回到公开年报核验；接口状态以真实业务响应为准；评测结果以生产 Run 记录为准；有争议的规则归因保留为 Bad Case。

## 2. 使用的 AI 能力

| 工具或模型 | 用途 | 输出如何验证 |
| --- | --- | --- |
| Codex 开发辅助 | 需求拆解、代码实现辅助、脚本执行、问题定位、材料校对 | 查看代码差异，执行本地测试，在正式 URL 手工复跑 |
| qwen3.7-flash | 产品中的语义评测、Prompt 候选回答生成和连通检查 | 结构化 schema、确定性规则、人工复核和 Run ID |
| 扶摇金融接口 | 在线行情诊断 | 同时检查 HTTP、业务码、字段和 request_id，不只看 HTTP 200 |

开发辅助工具的具体底层模型不作为作品结论的一部分；产品运行模型固定记录为 qwen3.7-flash。

## 3. 我与 AI 的分工

| 工作 | 我的责任 | AI 辅助内容 |
| --- | --- | --- |
| 选题 | 结合岗位要求和个人经历选择回答质量治理平台 | 对三个命题做匹配分析 |
| 范围 | 决定 24 小时内只做可运行闭环，砍掉多租户、自动调参等功能 | 提供优先级和风险清单 |
| 规则 | 确认意图标准、个性化不变量、硬阻断和人工复核边界 | 起草规则结构和边界案例 |
| 数据 | 选择公开年报和构造匿名问答/KYC，不使用真实用户隐私 | 辅助整理字段、换算和案例格式 |
| 实现 | 决定 EdgeOne 架构、环境变量、页面流程和最终功能 | 辅助编写前端、Functions 和测试代码 |
| 验收 | 在生产地址逐条运行案例、记录 Run、判断是否符合预期 | 对比预期与实际、定位代码和 Judge 问题 |
| 发布 | 配置密钥、部署、确认正式 URL 和公开材料 | 辅助做敏感信息扫描、文档一致性检查 |

## 4. 关键过程记录

| 阶段 | AI 辅助产出 | 我的核验或修正 | 最终决定 |
| --- | --- | --- | --- |
| 工期与范围 | 根据命题原文给出执行计划 | 实际只有 24 小时，不采用原题 48 小时安排 | 所有计划压缩为 24 小时，以闭环优先 |
| 托管选择 | 讨论 Cloudflare、静态托管和服务端函数 | Cloudflare 运行位置调用扶摇受限；国内自定义域名备案来不及 | 改用 EdgeOne Pages Functions，公开地址需海外 IP |
| 模型接入 | 对照千问兼容接口编写服务端调用 | 我确认实际模型为 qwen3.7-flash，并配置百炼环境变量 | 生成与评测固定记录模型和模式 |
| 环境变量排错 | 根据 health 结果检查代码读取项 | 发现控制台误写 `DASHSCOPCE_API_KEY` | 更正为 `DASHSCOPE_API_KEY`，重新部署后 ready=true |
| 金融接口 | 对无 Key 响应和文档进行比较 | HTTP 200 但业务码为失败，不能算数据成功 | 服务诊断同时检查业务码和 data |
| 数据证据 | 辅助读取年报和提取候选字段 | 我核对第 2、5 页，确认营业收入、利润、EPS 和分配预案 | 固定年报快照用于可复现回归 |
| 初始评测 | 帮助整理 v1 结果和错误模式 | 我逐条在生产页面运行并保存 Run ID | 7/8 总判决方向正确，归因和任务状态存在问题 |
| Judge 修复 | 根据 Bad Case 建议规则边界和确定性检查 | 我复测 C15/C18/C22/C23/C24/C28 | v2 核心判决与必需规则 6/6 命中，2 条仍部分符合 |
| Prompt 回归 | 辅助完善生成与复评流程 | 我发现旧页面只实时评测候选、未展示完整 Run 链 | 改为同一次操作实时评测基线和候选，展示三段 Run |
| 结果表达 | 辅助计算样本结果 | 我拒绝把小样本写成生产准确率，保留 C22/C28/C12 归因问题 | 报告分子、分母、Run 和残余 Bad Case |

## 5. 代表性纠错

### 5.1 HTTP 200 不等于金融数据成功

早期无 Key 请求返回 HTTP 200，但业务码和消息显示鉴权失败。最终产品没有按 HTTP 状态直接读取 data，而是继续检查业务码、结构和 request_id。这一纠错也被做成服务诊断的产品规则。

### 5.2 “回答安全”不等于“任务完成”

C18 在工具失败后明确拒绝编造数据。v1 将其判为证据不足且任务完成，混淆了安全降级和端到端成功。修复后质量为 pass、`task_completed=false`，没有因接口失败惩罚诚实回答，也没有把未取得数据算成任务成功。

### 5.3 总判决正确不代表归因正确

初次 C22/C23/C24/C28 多数都得到“不通过”，但规则 ID 漂移。只看总 verdict 会掩盖修复方向错误，因此我保留原 Run，在 v2 中同时检查必需规则、额外规则和任务完成状态。

### 5.4 Prompt 回归必须控制变量

旧版回归卡片左侧使用预设标签，右侧只显示候选评测，不足以证明同条件比较。我要求改为一次操作完成基线实评、候选生成和候选复评，并记录 Run `8fac9186`、`57a98e69`、`1244815a`。这使“Prompt 修复”从展示性对比变成可追溯实验。

## 6. 验证方式

### 6.1 事实验证

- 关键财务数据回到贵州茅台 2024 年年度报告第 2、5 页。
- 元转亿元使用十进制换算，不用统一百分比容差放过数量级错误。
- 报告期、披露时间和提问时点分别记录。

### 6.2 代码验证

- `npm test` 执行函数冒烟测试。
- `npm run check` 检查前端和 Functions 语法。
- 发布前扫描硬编码 Key、旧模型名、本地绝对路径和历史托管口径。

### 6.3 生产验证

- 在正式 URL 真实调用扶摇和 qwen3.7-flash。
- 手工运行 C01、C02 和 6 条 v2 边界回归。
- 验证输入超长、历史非 JSON、人工复核刷新和用户反馈。
- 完成 C12 三段 Run 的同条件 Prompt 回归。

## 7. 没有交给 AI 决定的事项

- 是否将一条机器 finding 视为正确标签。
- 金融数据真实性、来源权限和监管合规结论。
- 高风险画像是否可以省略风险或修改事实。
- 是否把某次回归写成准确率提升。
- 是否上线、自动改 Prompt 或自动生成交易建议。

## 8. 已知风险

- 初始案例标签也有 AI 辅助参与，尚未经过多名金融专家一致性标注。
- 生成和评测使用同一模型供应商，可能存在自评偏差。
- 作品样本较小，规则修复可能对未测试表达产生误报或漏报。
- AI 辅助编写代码提高了速度，但不能替代安全审计、压力测试和长期维护。

因此，作品中保留人工复核、确定性规则、版本信息和真实失败状态，并将残余错误公开写入测试报告。
